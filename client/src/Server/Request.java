package Server;

/**
 * @author kasper & mads
 * Class defines the request a client can send to a server
 * GET_PERSONS should return all persons currently in the DB
 * ADD_PERSON will accept a person object and add it to the DB
 */
public enum Request {
    GET_PERSONS,
    ADD_PERSON
    
    
}
